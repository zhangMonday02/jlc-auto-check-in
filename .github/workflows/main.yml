name: 嘉立创自动签到主脚本

on:
  workflow_dispatch:  # 手动触发
  schedule:
    - cron: '0 05 * * *'
    - cron: '0 13 * * *'
    - cron: '0 21 * * *'
    # 每天北京时间9点运行（UTC时间01点）
    # 实际运行可能延迟1小时内，取决于GitHub资源

env: # 公共推送环境变量（所有job共享，从secrets读取）
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
  WECHAT_WEBHOOK_KEY: ${{ secrets.WECHAT_WEBHOOK_KEY }}
  DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}
  PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
  SERVERCHAN_SCKEY: ${{ secrets.SERVERCHAN_SCKEY }}
  COOLPUSH_SKEY: ${{ secrets.COOLPUSH_SKEY }}
  CUSTOM_WEBHOOK: ${{ secrets.CUSTOM_WEBHOOK }}

jobs:
  # 第一个签到任务（账号组1）
  running-1:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组1)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_1 }}" "${{ secrets.JLC_PASSWORD_1 }}" "${{ secrets.ERROR_1 }}"

  # 第二个签到任务（账号组2）
  running-2:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组2)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_2 }}" "${{ secrets.JLC_PASSWORD_2 }}" "${{ secrets.ERROR_2 }}"

  # 第三个签到任务（账号组3）
  running-3:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组3)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_3 }}" "${{ secrets.JLC_PASSWORD_3 }}" "${{ secrets.ERROR_3 }}"

  # 第四个签到任务（账号组4）
  running-4:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组4)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_4 }}" "${{ secrets.JLC_PASSWORD_4 }}" "${{ secrets.ERROR_4 }}"

  # 第五个签到任务（账号组5）
  running-5:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组5)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_5 }}" "${{ secrets.JLC_PASSWORD_5 }}" "${{ secrets.ERROR_5 }}"

          
 # 第六个签到任务（账号组6）
  running-6:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组6)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_6 }}" "${{ secrets.JLC_PASSWORD_6 }}" "${{ secrets.ERROR_6 }}"

  
  # 第七个签到任务（账号组7）
  running-7:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组7)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_7 }}" "${{ secrets.JLC_PASSWORD_7 }}" "${{ secrets.ERROR_7 }}"

  # 第八个签到任务（账号组8）
  running-8:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组8)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_8 }}" "${{ secrets.JLC_PASSWORD_8 }}" "${{ secrets.ERROR_8 }}"

    # 第九个签到任务（账号组9）
  running-9:
    runs-on: ubuntu-22.04
    steps:
      - name: '检出代码'
        uses: actions/checkout@v3
      - name: '设置Python环境'
        uses: actions/setup-python@v5
        with:
          python-version: 3.7
      - name: '设置时区为北京时间'
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "当前时间: $(date)"
      - name: '安装依赖'
        run: |
          pip install --upgrade pip
          pip install -r ./requirements.txt
      - name: '准备Chrome驱动'
        uses: nanasess/setup-chromedriver@v2
      - name: '进行签到流程(账号组9)'
        run: |
          python ./jlc.py "${{ secrets.JLC_USERNAME_9 }}" "${{ secrets.JLC_PASSWORD_9 }}" "${{ secrets.ERROR_9 }}"

